import RESTService from '../utils/rest-service'

class BooksService extends RESTService {
  // extend service implementation as needed
}

export default new BooksService('http://localhost:4000/books')
