import RESTService from '../utils/rest-service'

class AuthorsService extends RESTService {
  // extend service implementation as needed
}

export default new AuthorsService('http://localhost:4000/authors')
